请仔细分析这张图片中的所有题目，并以 JSON 数组格式返回识别结果。

对于每道题目，请返回以下字段：

```json
[
  {
    "index": 1,
    "question": "完整的题目文本，包含题号和所有选项（如有）",
    "wrongAnswer": "从图中识别到的学生错误答案，如无则为空字符串",
    "correctAnswer": "你给出的正确答案",
    "analysis": "简明扼要的解题思路",
    "subjectGuess": "推测的学科，如：语文、数学、英语、物理、化学、生物、历史、地理、政治",
    "bbox": {
      "x": 0.0,
      "y": 0.0,
      "width": 1.0,
      "height": 0.1
    }
  }
]
```

关于 `bbox` 坐标说明：
- 所有值为 0 到 1 之间的归一化小数，表示题目在图片中的相对位置
- `x`, `y` 为题目区域左上角坐标
- `width`, `height` 为题目区域的宽和高

要求：
1. 只返回 JSON 数组，不要包含 markdown 代码块标记或其他文字
2. 仔细识别每道题的完整内容，不要遗漏
3. 如果图中有学生写的答案且明显是错误的，填入 wrongAnswer
4. correctAnswer 要给出完整正确的答案
5. analysis 要简洁但有教育意义，帮助学生理解解题思路