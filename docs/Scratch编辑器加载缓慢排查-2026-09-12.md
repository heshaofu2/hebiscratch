## 结论

2026-09-12 对 `https://aqian.bibridge.top` 的实际检查表明，已复现的主要问题是编辑器启动文件下载缓慢。服务器内部读取正常；当前电脑到部署节点的实测下载速度约为 20–24 KB/s。一次浏览器测试从 20:37:18 开始请求，到 20:43:05 完成主脚本传输，随后编辑器正常显示默认角色。

这不是已经证实的数据库故障或项目文件损坏。上述结论针对本次复现，尚不能排除用户遇到其他加载后问题。

## 验证结果

- Scratch 的 7 个容器均处于运行状态；磁盘使用率 46%，内存未耗尽。
- 最近打开项目的后端接口均返回 HTTP 200。
- 抽查最近访问的 4 个项目：SB3 压缩包校验、project.json 解析通过；每个项目只有一个舞台，分别有 1、4、3、5 个角色；造型和声音文件均完整。
- 线上 `embedded.js` 原始大小为 27,402,260 字节，是开发构建。实际 GET 响应已经启用 gzip，传输约 7,116,369 字节；不能仅凭 HEAD 没有 Content-Encoding 就判定没有压缩。
- 服务器通过本机 HTTPS 请求该脚本：2.35 秒完成。
- 当前电脑请求该脚本：45 秒收到 900,445 字节，约 20 KB/s；排除环境代理后的另一轮测试为 30 秒收到 720,256 字节，约 24 KB/s。
- 浏览器等待完整传输后，线上编辑器正常显示默认角色。

## 已准备的改动

`build-scratch.sh` 显式使用 `NODE_ENV=production` 构建；`frontend/Dockerfile` 中的手动构建示例同步修改。

实际正式构建成功，`embedded.js` 原始大小为 18,336,419 字节，gzip 后为 6,052,034 字节。与线上传输量相比仅减少约 15%，无法单独解决当前线路缓慢的问题。正式构建在本地浏览器可打开；四个实际项目文件依次加载全部返回 success=true，角色数量与文件检查一致。测试未保存或修改这些项目。

构建过程只有资源体积警告，没有构建错误；脚本语法与 Git 差异检查通过。初次排查时未提交或推送代码。

## 线上替换前的限制

初次排查发现，线上构建的 `embedded.jsx` 含有本地当前源码没有的目标数量检查与清理逻辑；storage、hash-parser、project-fetcher 三个相关模块的源码校验一致。因此当时没有直接把本地新构建覆盖到线上。

后续发布前核对确认：线上 `embedded.jsx` 的 SHA-256 为 `c684caae71288d0381a9388c23b6e0a25f5b07726ab580cc4e267d1208df90f5`，与 scratch-gui 提交 `c0275d1` 完全一致。本地后续提交 `d2b2c45` 只修改了该文件，移除了上述防御逻辑。本次发布选用独立工作目录中的 `c0275d1` 进行生产构建，保留线上加载行为；当前开发目录不切换版本。

## 后续可验证步骤

1. 优先对当前电脑与服务器之间的传输做对照测试，比较完整下载时间与平均速度，而不是只检查 HTTP 200。
2. 服务器当前使用 cubic，内核存在 tcp_bbr 模块。可以在用户确认后做临时拥塞控制对照测试：记录原值，加载该模块，临时设置 `net.ipv4.tcp_congestion_control=bbr`，用新连接重新测量相同文件，结束后恢复 cubic；第一轮不写持久配置。该设置影响整台服务器的新 TCP 连接，需考虑同机其他服务。此方案尚未执行，也不保证能改善当前线路。
3. 上线构建前先核对并保留线上已有的加载修复，再验证新建、打开已有项目及保存后重开。

## 生产发布记录

- 切换时间：2026-09-12 23:01（北京时间）。
- 主仓库部署提交：`df64e92`；Scratch 编辑器源码版本：`c0275d1`，在独立工作目录中以生产模式构建。
- 发布标识：`scratch-production-20260912145050`。
- 前端镜像：`sha256:d1b59754f6790a397172a403fb00cb130af0617449ba3a95644c3cc58cbbc94a`。
- `embedded.js` 大小：18,340,942 字节；实际 HTTP gzip 传输量：6,062,089 字节。
- `embedded.js` SHA-256：`656b116732ed7cf32b6bf72771f68d7764eb83a9e51f618b380cec6d39684e96`。
- 服务器备份目录：`/opt/backups/scratch/scratch-production-20260912145050/`，包含原静态文件、容器清单、原镜像 ID 和构建日志。
- 原镜像已保留为 `scratch-frontend:rollback-scratch-production-20260912145050`，对应 `sha256:6bf6d9f3800d21ec2f7f79386ec2c222e0414e6cc4152f28c3ac5b3c976ed03b`。

发布时使用现有 Webhook 部署锁避免推送触发并发构建，服务器使用两份 Compose 配置，只重建并更新 `scratch-frontend`。

### 已完成的验证

- 发布包在本地浏览器中新建项目正常显示默认角色。
- 四个实际项目均完成加载、导出 SB3、重开、再次导出的往返验证，角色、积木、造型、声音数量与原文件一致。验证只操作本地文件副本，未写入线上账号的项目。
- 本地发布包与线上运行容器中的全部 1,697 个静态文件 SHA-256 一致。
- 服务器内通过 HTTPS 完整下载脚本成功，耗时 1.69 秒，校验值一致。
- 当前电脑通过公网完整下载同一脚本成功，gzip 传输 6,062,089 字节，耗时 354.26 秒，平均约 17 KB/s，SHA-256 一致。该请求与浏览器验收存在时间重叠，记录用于证明公网传输完整及仍有明显延迟，不作为严格的构建前后性能对照。
- 线上浏览器加载 `/scratch/embedded.html?mode=new` 后正常显示编辑器、默认角色与舞台，确认生产版脚本可以执行。验收浏览器未登录账号，未直接执行账号内的保存操作；项目导出与重开使用同一发布包在本地完成。
- 首页、`/health`、`/ai-course/`、`/word-warden/` 返回 200；未登录请求 `/api/auth/me` 返回预期的 401，后端健康接口返回 `{"status":"ok"}`；已下线的 `/ai-shifu-for-school/` 保持 410。
- 与发布前容器清单比对，只有前端容器发生替换，其余原有容器均保持运行。
